<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Tiered Puzzle Game</title>
  <style>
    :root {
      --bg: #f7f8fb;
      --card: #ffffff;
      --accent: #4f46e5;
      --muted: #6b7280;
      --radius: 12px;
      --pad: 20px;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }

    body {
      margin: 0;
      background: linear-gradient(180deg, var(--bg), #eef2ff);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      color: #111827;
    }

    .card {
      background: var(--card);
      border-radius: var(--radius);
      box-shadow: 0 8px 30px rgba(17,24,39,0.06);
      width: min(720px, 92vw);
      padding: calc(var(--pad) * 1.2);
    }

    h1 {
      margin: 0 0 8px 0;
      font-size: 1.6rem;
      color: var(--accent);
      letter-spacing: -0.01em;
    }

    p.lead {
      margin: 0 0 16px 0;
      color: #111827;
      line-height: 1.45;
    }

    p.small {
      margin: 0 0 18px 0;
      color: var(--muted);
      font-size: 0.95rem;
    }

    .meta {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 18px;
    }

    .chip {
      background: #f3f4f6;
      color: #374151;
      padding: 8px 12px;
      border-radius: 999px;
      font-size: 0.9rem;
    }

    .actions {
      display: flex;
      gap: 12px;
      align-items: center;
    }

    button.primary {
      background: var(--accent);
      color: white;
      border: none;
      padding: 10px 16px;
      border-radius: 10px;
      font-weight: 600;
      cursor: pointer;
      box-shadow: 0 6px 18px rgba(79,70,229,0.18);
    }

    button.secondary {
      background: transparent;
      border: 1px solid #e6e7ff;
      padding: 10px 14px;
      border-radius: 10px;
      cursor: pointer;
      color: #374151;
    }

    footer {
      margin-top: 18px;
      color: var(--muted);
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <main class="card" role="main" aria-labelledby="title">
    <h1 id="title">Tiered Puzzle Game</h1>

    <p class="lead">
      The Tiered Puzzle Game is a simple, interactive word-guessing game made with Python.
      Players get a short clue and a few chances to guess the word — the game gets tougher
      across Easy, Medium, and Hard levels.
    </p>

    <p class="small">
      This demo page contains the project introduction and a placeholder start button.
      Hook it up to your Python backend or add JavaScript to implement gameplay in the browser.
    </p>

    <div class="meta" aria-hidden="true">
      <div class="chip">3 Levels: Easy → Medium → Hard</div>
      <div class="chip">Scoring & Leaderboard</div>
      <div class="chip">File handling (player records)</div>
    </div>

    <div class="actions">
      <button class="primary" onclick="alert('Game logic runs in Python. Integrate with backend or add JS to continue.')">
        ▶️ Start Game
      </button>
      <button class="secondary" onclick="alert('You can add rules or instructions here.')">How to play</button>
    </div>

    <footer>
      Tip: To make this interactive in the browser, replace the alerts with JavaScript that
      runs the game logic or connects to a Python server.
    </footer>
  </main>
</body>
</html>
